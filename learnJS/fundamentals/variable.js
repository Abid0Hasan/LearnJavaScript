/**
 * Variable  is just simply a place holder of differnt
 * Type of Data. We can assume it as like a box which can
 * hold differnt type of values as well. 
 */

 //Declare a variable
 var name= "Abid"; //String Type
 var age = 27;  //Number Type
 var hasDrivingLisence = true; //Boolean Type
 var empty = undefined; //The variable has no value.
 
 //How to findout the type of variable
 log(name +"Type = "+ typeof name);
 log(age +"Type = "+ typeof age);
 log(hasDrivingLisence +"Type = "+ typeof hasDrivingLisence);
 log(empty +"Type =  "+ typeof empty);